{
  "name": "llm-client",
  "version": "1.0.0",
  "description": "Shared LLM wrapper — Claude (default) or OpenAI, selected by env key",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.27.3",
    "openai": "^4.47.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  }
}
